<head>
    <title>Deregister</title>
    <link rel="stylesheet" href="{{ url_for('static',filename='css/deletion.css') }}">
</head>
<body>
    <div class = "container">
        <h3 id = "HEADING">
            > Are you sure you want to deregister the {{display_args['person_type']}} named {{display_args['person_name']}} from the event {{display_args['event_name']}}
        </h3>
        <a href = "{{url_for('deregister', pid = pid, event_id = event_id)}}"><button class = "dropbtn">Confirm</button></a>
        <a href = "{{url_for('event', event_id = event_id)}}"><button class = "dropbtn">Back</button></a>
    </div>
</body>